/**
 * Metric label eyebrow — small uppercase text above a large numeric value.
 * Used in SummaryGrid and WeakSpotSummary.
 */
export const TEXT_METRIC_LABEL_CLASS =
  "text-[11px] font-medium uppercase tracking-wide text-slate-400";

/**
 * Metric value — large numeric display text.
 * Used in SummaryGrid, WeakSpotSummary, and QuizCompletedState.
 */
export const TEXT_METRIC_VALUE_CLASS = "text-2xl font-semibold text-slate-900";
